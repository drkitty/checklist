hash_rounds = 20000

app_config = {
    'debug': True,
    'secret_key': '',
}

database = {
    'host': 'localhost',
    'user': 'checklist',
    'passwd': None,
    'db': 'checklistdb',
    'echo': True,
}
